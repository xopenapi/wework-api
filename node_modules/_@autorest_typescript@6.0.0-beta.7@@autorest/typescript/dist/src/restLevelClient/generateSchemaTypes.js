"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.generateSchemaTypes = void 0;
const generateObjectTypes_1 = require("./generateObjectTypes");
/**
 * Generates types to represent schema definitions in the swagger
 */
function generateSchemaTypes(model, project) {
    // Track models that need to be imported
    const importedModels = new Set();
    const modelsFile = project.createSourceFile(`src/models.ts`, undefined, {
        overwrite: true
    });
    const objectsDefinitions = generateObjectTypes_1.buildObjectInterfaces(model, importedModels);
    const objectTypeAliases = generateObjectTypes_1.buildPolymorphicAliases(model);
    modelsFile.addInterfaces(objectsDefinitions);
    modelsFile.addTypeAliases(objectTypeAliases);
}
exports.generateSchemaTypes = generateSchemaTypes;
//# sourceMappingURL=generateSchemaTypes.js.map