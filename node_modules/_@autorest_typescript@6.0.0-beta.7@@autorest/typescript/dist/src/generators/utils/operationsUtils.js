"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.calculateMethodName = void 0;
const nameUtils_1 = require("../../utils/nameUtils");
function calculateMethodName(operation) {
    const name = `${operation.namePrefix || ""}${nameUtils_1.normalizeName(operation.name, nameUtils_1.NameType.Property)}`;
    const firstLetter = name.substr(0, 1)[0].toUpperCase();
    return operation.isLro && operation.pagination === undefined
        ? `begin${firstLetter}${name.substr(1)}`
        : name;
}
exports.calculateMethodName = calculateMethodName;
//# sourceMappingURL=operationsUtils.js.map